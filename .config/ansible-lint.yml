---
# .ansible-lint
# See https://ansible-lint.readthedocs.io/configuring/

warn_list:
  - command-instead-of-module
  - no-changed-when
  - command-instead-of-shell

extra_vars:
  targets: localhost
