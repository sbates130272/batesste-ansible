---
- name: Apply netplan
  ansible.builtin.command:
    cmd: netplan apply
  become: true
  changed_when: true

- name: Restart libvirtd
  ansible.builtin.systemd:
    name: libvirtd
    state: restarted
    daemon_reload: true
  become: true
