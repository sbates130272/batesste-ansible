---
# Test playbook for the rocm_hipfile_setup role (deprecated).
# Verifies that the deprecation stub runs cleanly when
# rocm_hipfile_setup_allow_deprecated: true is set.
- name: The test playbook for the rocm_hipfile_setup role
  hosts: all
  roles:
    - role: rocm_hipfile_setup
      become: false
      rocm_hipfile_setup_allow_deprecated: true
