---
minor_changes:
  - >-
    Replace separate playbooks/setup-*.yml with a single playbooks/setup.yml that
    selects flows via recipe tags (recipe_newmachine, recipe_amd,
    recipe_aws_rocm_xio, etc.). run-ansible defaults to TAGS=recipe_newmachine.
