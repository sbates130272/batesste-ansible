---
# Copyright (c) Stephen Bates, 2026
#
# SPDX-License-Identifier: Apache-2.0
#
minor_changes:
  - >-
    Replace separate ``playbooks/setup-*.yml`` playbooks with a single
    ``playbooks/setup.yml`` that selects flows via ``setup_recipe`` values
    (``newmachine``, ``amd``, etc.). ``run-ansible`` defaults to
    ``RECIPE=newmachine``.
