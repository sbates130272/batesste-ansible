# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
# Generated by generate-workflows.py for role: claude_setup
# To regenerate: python3 .github/generate-workflows.py

name: claude_setup CI
'on':
  workflow_dispatch: {}
  pull_request:
    branches:
      - main
    paths:
      - roles/claude_setup/**
      - .github/workflows/claude_setup-ci.yml
      - requirements.txt
      - requirements.yml

jobs:
  claude_setup-test:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Install pip packages
        run: python3 -m pip install -r requirements.txt

      - name: Run ansible-galaxy to install collections
        run: ansible-galaxy collection install -r requirements.yml

      - name: Build and install the collection locally
        run: |
          ansible-galaxy collection build --force
          ansible-galaxy collection install sbates130272-batesste-*.tar.gz --force

      - name: Create an SSH keypair
        run: mkdir -p .ssh && ssh-keygen -b 2048 -t rsa -f ~/.ssh/id_rsa -q -N ""

      - name: Create a GNU PGP folder
        run: mkdir -p .gnupg

      - name: Write hosts-ci file
        uses: DamianReeves/write-file-action@v1.3
        with:
          path: ./roles/claude_setup/hosts-ci
          write-mode: overwrite
          contents: |
            all:
              hosts:
                localhost:
                  ansible_connection: local
                  ansible_user: runner
                  vault_claude_setup_api_key: test_ci_api_key_placeholder
                  claude_setup_proxy_upstream: http://localhost:8000
                  claude_setup_proxy_port: 8888

      - name: Run the test playbook against the local runner
        run: ansible-playbook -v -i hosts-ci ./tests/test.yml
        working-directory: ./roles/claude_setup
        env:
          ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles

      - name: Verify claude_setup installation
        run: |
          node --version || true
          npm --version || true
          which claude || npm list -g @anthropic-ai/claude-code || true
          nginx -v || true
          systemctl status nginx || true
          cat ~/.claude/settings.json || true
          ss -tlnp | grep 8888
          curl -v --proxy http://127.0.0.1:8888 https://llm-api.amd.com/Anthropic || true
