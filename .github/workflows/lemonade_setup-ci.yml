# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
# Generated by generate-workflows.py for role: lemonade_setup
# To regenerate: python3 .github/generate-workflows.py

name: lemonade_setup CI
'on':
  workflow_dispatch: {}
  pull_request:
    branches:
      - main
    paths:
      - roles/lemonade_setup/**
      - .github/workflows/**
      - requirements.txt
      - requirements.yml

jobs:
  lemonade_setup-test:
    runs-on: ubuntu-24.04
    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@v1.3.1
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Install pip packages
        run: python3 -m pip install -r requirements.txt

      - name: Run ansible-galaxy to install collections
        run: ansible-galaxy collection install -r requirements.yml

      - name: Build and install the collection locally
        run: |
          ansible-galaxy collection build --force
          ansible-galaxy collection install sbates130272-batesste-*.tar.gz --force

      - name: Create an SSH keypair
        run: mkdir -p .ssh && ssh-keygen -b 2048 -t rsa -f ~/.ssh/id_rsa -q -N ""

      - name: Create a GNU PGP folder
        run: mkdir -p .gnupg

      - name: Write hosts-ci file
        uses: DamianReeves/write-file-action@v1.3
        with:
          path: ./roles/lemonade_setup/hosts-ci
          write-mode: overwrite
          contents: |
            all:
              hosts:
                localhost:
                  ansible_connection: local
                  ansible_user: runner
                  lemonade_setup_start_service: false
                  lemonade_setup_preload_enabled: false
                  lemonade_setup_exporter_enabled: false
                  vault_lemonade_setup_api_key: test_ci_api_key_placeholder
                  rocm_setup_run_checks: false
                  rocm_setup_install_metrics_exporter: false
                  rocm_setup_skip_reboot: true

      - name: Run the test playbook against the local runner
        run: ansible-playbook -v -i hosts-ci ./tests/test.yml
        working-directory: ./roles/lemonade_setup
        env:
          ANSIBLE_ROLES_PATH: ${{ github.workspace }}/roles

      - name: Verify lemonade_setup installation
        run: |
          which lemonade-server || true
          dpkg -l | grep lemonade || true
          dpkg -l | grep rocm || true
          cat /etc/lemonade/lemonade.conf || true
          systemctl status lemonade-server || true
